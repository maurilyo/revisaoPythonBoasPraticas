[project]
name = "revisaopythonboaspraticas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.13"
dependencies = [
    "pre-commit>=4.6.2",
    "ruff>=0.16.2",
]

[tool.ruff.lint]
# E = Erros, F = Pyflakes(Lógica), N = Naming (Nomenclatura PEP-8)
select = ["E", "F", "N"]
